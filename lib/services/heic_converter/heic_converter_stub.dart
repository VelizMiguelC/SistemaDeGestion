import 'dart:typed_data';

const bool puedeConvertirHeic = false;

Future<Uint8List> convertirHeicAJpeg(Uint8List heicBytes) {
  throw UnsupportedError('La conversión de HEIC solo está implementada en la Web.');
}
